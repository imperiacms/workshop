<root>
    <common>    
        # ----------------------------------------------------------------------
        # filename
        # can be overwritten in the navigation element
        # ----------------------------------------------------------------------
        # ssi-filename creation / args:
        #   <!--directory-->    e.g. /cms01/intra
        #   <!--node_id-->      e.g. /10/17/29
        #   <!--cat_id-->       e.g. 17
        #   <!--lingua-->       e.g. de
        #   <!--nav_element-->  e.g. h_nav
        filename = /mt/ssi/<!--nav_element-->/<!--doc_id-->.ssi
    </common>
    <breadcrumb>
        file_startlevel = "0"
        file_maxlevel = "10"

        content_maxlevel = "10"

        li_snippet <<EOF
        EOF

        ul_snippet <<EOF
        EOF

    </breadcrumb>
</root>
